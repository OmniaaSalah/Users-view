import { ScopePermitDirective } from './scope-permit.directive';

describe('ScopePermitDirective', () => {
  it('should create an instance', () => {
    const directive = new ScopePermitDirective();
    expect(directive).toBeTruthy();
  });
});
