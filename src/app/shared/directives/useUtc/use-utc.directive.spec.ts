import { UseUtcDirective } from './use-utc.directive';

describe('UseUtcDirective', () => {
  it('should create an instance', () => {
    const directive = new UseUtcDirective();
    expect(directive).toBeTruthy();
  });
});
