import { TrauncateTextPipe } from './trauncate-text.pipe';

describe('TrauncateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TrauncateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
