import { FormControlValidationMsgDirective } from './form-control-validation-msg.directive';

describe('FormControlValidationMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlValidationMsgDirective();
    expect(directive).toBeTruthy();
  });
});
