import { LanguageRestrictionDirective } from './number-restriction.directive';

describe('LanguageRestrictionDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageRestrictionDirective();
    expect(directive).toBeTruthy();
  });
});
