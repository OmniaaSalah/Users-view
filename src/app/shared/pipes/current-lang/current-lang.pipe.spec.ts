import { CurrentLangPipe } from './current-lang.pipe';

describe('CurrentLangPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentLangPipe();
    expect(pipe).toBeTruthy();
  });
});
